# greet

A tiny Python utility for generating greetings and adding numbers.

## Usage

```python
from greet import greet, add_numbers

greet("world")       # "Hello, world!"
add_numbers(2, 3)     # 5
```

## Running tests

```
python -m unittest discover -s tests
```
